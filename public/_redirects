# Redirect www to non-www (canonical)
https://www.sgindustrialproperties.com/* https://sgindustrialproperties.com/:splat 301!

# Admin panel — serve static files first (before SPA catch-all)
/admin /admin/index.html 200
/admin/ /admin/index.html 200
/admin/* /admin/:splat 200

# Netlify Functions
/.netlify/functions/* /.netlify/functions/:splat 200

# SPA fallback (last — only matches if nothing above matched)
/* /index.html 200
